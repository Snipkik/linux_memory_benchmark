# linux_memory_benchmark
Measuring/Evaluating the performance of a computer system using benchmarks

# build 

simply run the command `./build.sh` 
It will execute the makefile, run the benchmarks and create charts
